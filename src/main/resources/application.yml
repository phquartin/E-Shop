spring:
  application:
    name: basketservice

  data:
    mongodb:
      host: localhost
      port: 27017
      database: basketservice
    redis:
      host: localhost
      port: 6379
      password: sa
  cache:
    redis:
      time-to-live: 60000

platzi:
  store:
    client:
      api: https://api.escuelajs.co/api/v1/